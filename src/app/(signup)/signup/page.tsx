import SignUpForm from '@/cmps/SignUpForm'
import { FC } from 'react'

interface pageProps {
  
}

const page: FC<pageProps> = ({}) => {
  return <div>
    <SignUpForm />
  </div>
}

export default page